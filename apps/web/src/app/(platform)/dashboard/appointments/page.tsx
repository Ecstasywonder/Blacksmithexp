/** Appointment inbox placeholder; mutations belong in tested domain use cases. */
export default function AppointmentsPage() {
  return (
    <section>
      <p className="eyebrow">Appointment inbox</p>
      <h1>Appointments</h1>
      <p>Pending requests will appear here oldest first, with confirm and decline actions.</p>
    </section>
  );
}
